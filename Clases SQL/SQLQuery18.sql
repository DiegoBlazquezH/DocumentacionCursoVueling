-- Ejecutar Variable del Sistema.
select @@VERSION; -- Versión del Motor.
select @@LANGUAGE; -- lenguaje que se utiliza
select @@MAX_CONNECTIONS; -- Máxima conexiones que se pueden ejecutar en Sql.
select @@OPTIONS; -- información valores actuales opciones set.
select @@REMSERVER; -- nombre servidor contenido registro nombre acceso a serviodr remoto
select @@SERVERNAME; -- Nombre Servidor Local.
select @@TOTAL_WRITE; -- número de escritura de datos en disco.
select @@TOTAL_READ; -- número de Lecturas de datos en disco.
select @@TRANCOUNT; -- número de transacciones activas actualmente para el usuario actual.



